# Pet-Adoption-Likelihood-Predictor
A machine learning project that predicts the likelihood of pet adoption using Decision Tree classifiers. It involves data preprocessing, feature scaling, and hyperparameter tuning to achieve 91% accuracy.
